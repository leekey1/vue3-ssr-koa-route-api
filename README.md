# vue3-ssr-koa-route-api

# build client entry
npm run build-client

# build server entry
npm run build-server

# build both
npm run build

# run server
node server

# test
in terminal 
  curl http://localhost:8080/list
